server:
  port: 5000
spring:
  datasource:
    url: ${DB_URL}
    username:  ${DB_USER}
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
          timeout: '3000'
          auth: 'true'
          connectiontimeout: '5000'
          writetimeout: '5000'
    host: smtp.gmail.com
    username: bubbleatrevature@gmail.com
    password: R3Vature!
    port: '587'
cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_ACCESS_KEY}

    region:
      static: us-east-1
    stack:
      auto: false

application:
  bucket:
    name: ${BUCKET_NAME}